# ==================================================================
# REQUIRED ENVIRONMENT VARIABLES
# ==================================================================

# Supabase Configuration (Required)
# Get these from your Supabase project settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ==================================================================
# OPTIONAL ENVIRONMENT VARIABLES
# ==================================================================

# App URL (Required for production, defaults to localhost in development)
# Used for webhook callbacks and internal API calls
NEXT_PUBLIC_APP_URL=https://your-app-domain.com

# ==================================================================
# DOCKER CONFIGURATION (Local Evolution API Setup)
# ==================================================================

# PostgreSQL Database Configuration
POSTGRES_USER=evolution
POSTGRES_PASSWORD=evolution_secure_password_change_this
POSTGRES_DB=evolution

# Evolution API Configuration
# Internal URL for Docker network communication (do not change unless you know what you're doing)
EVOLUTION_API_URL=http://evolution-api:8080
# Generate a secure random API key (minimum 32 characters)
EVOLUTION_API_KEY=change_this_to_a_secure_random_key_minimum_32_chars
# External server URL (used for webhooks and QR codes)
EVOLUTION_SERVER_URL=http://localhost:8080

# Authentication (Optional - for NextAuth if using)
NEXTAUTH_URL=http://localhost:5000
NEXTAUTH_SECRET=your-nextauth-secret-key

# ==================================================================
# DEVELOPMENT ONLY (Do not use in production)
# ==================================================================

# Node Environment
NODE_ENV=development

# Security Headers (Development Only)
# Set to 'true' to disable security headers in development (e.g., for iframe support)
# IMPORTANT: Never set to 'true' in production!
DISABLE_SECURITY_HEADERS=false

# Allowed Development Origins (comma-separated)
# Used for CORS and iframe support in development environments
ALLOWED_DEV_ORIGINS=http://localhost:3000,http://localhost:5000

# ==================================================================
# PRODUCTION CHECKLIST
# ==================================================================
# ✓ Set NODE_ENV=production
# ✓ Use HTTPS URLs for NEXT_PUBLIC_APP_URL
# ✓ Never set DISABLE_SECURITY_HEADERS=true
# ✓ Ensure SUPABASE_SERVICE_ROLE_KEY is kept secure and never exposed
# ✓ Use strong, randomly generated secrets for NEXTAUTH_SECRET
# ✓ Enable RLS (Row Level Security) on all Supabase tables
# ✓ Set up proper CORS policies in Supabase
# ✓ Enable rate limiting if needed
# ✓ Set up monitoring and error tracking